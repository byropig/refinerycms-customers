<ul>
  <% if Admin::CustomersController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_customers_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_customer_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::CustomersController.sortable? and Customer.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Customers"),
                 admin_customers_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Customers"),
                 admin_customers_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>